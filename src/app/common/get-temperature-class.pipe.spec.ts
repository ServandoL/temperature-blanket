import { GetTemperatureClassPipe } from './get-temperature-class.pipe';

describe('GetTemperatureClassPipe', () => {
  it('create an instance', () => {
    const pipe = new GetTemperatureClassPipe();
    expect(pipe).toBeTruthy();
  });
});
