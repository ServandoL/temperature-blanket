import { MissingDaysPipe } from './missing-days.pipe';

describe('MissingDaysPipe', () => {
  it('create an instance', () => {
    const pipe = new MissingDaysPipe();
    expect(pipe).toBeTruthy();
  });
});
